//! Typed decode of the owner's payloads. Task 3 replaces both placeholders with
//! the real `/api/tui` and regime shapes; they exist now only so `bus` compiles.

pub struct Snapshot;

pub struct RegimePanel;
